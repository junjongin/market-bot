name: Schedule Test

on:
  schedule:
    - cron: "*/12 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Show trigger
        run: |
          echo "Triggered by ${{ github.event_name }}"
          date -u
